// Entry point: Import the root component.
// The root component will handle importing other necessary global components.
import './components/app-root.js';

// Register service worker if needed (omitted for simplicity)
console.log('Jorkhon App Initialized');